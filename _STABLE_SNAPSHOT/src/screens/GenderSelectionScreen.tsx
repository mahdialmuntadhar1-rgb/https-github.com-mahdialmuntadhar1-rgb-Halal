import React from 'react';
import { AppLanguage } from '../types';
import { TRANSLATIONS } from '../lib/translations';
import GenderSelectionCards from '../components/GenderSelectionCards';
import { Heart } from 'lucide-react';

interface GenderSelectionScreenProps {
  locale: AppLanguage;
  onSelectGender: (gender: 'male' | 'female') => void;
}

export default function GenderSelectionScreen({ locale, onSelectGender }: GenderSelectionScreenProps) {
  const t = TRANSLATIONS[locale] || TRANSLATIONS['ar'];
  const txt = (en: string, ar: string, ckb: string) => {
    return locale === 'en' ? en : locale === 'ckb' ? ckb : ar;
  };

  return (
    <div className="min-h-[80vh] flex flex-col items-center justify-center px-4 py-12 relative" id="gender-selection-screen">
      <div className="max-w-4xl w-full text-center space-y-6">
        <div className="inline-flex p-3 bg-accent-coral/10 rounded-full text-accent-coral animate-pulse">
          <Heart className="w-8 h-8 fill-current" />
        </div>
        
        <h2 className="text-3xl sm:text-4xl font-serif font-black text-warm-charcoal tracking-tight font-display">
          {txt('Begin Your Marriage Journey', '???? ????? ????????', '??????????? ????? ??????')}
        </h2>
        
        <p className="text-sm sm:text-base text-[#6B635B] font-medium leading-relaxed max-w-2xl mx-auto">
          {txt(
            'To maintain maximum privacy, safety, and match compatibility inside Iraq, please specify your gender identity. Your profile parameters will automatically adapt to respect Zawaj traditions.',
            '?????? ??? ???? ????? ???????? ??????? ?????? ??????? ?? ??????? ???? ????? ????. ?????? ??????? ????? ???????? ??? ?????? ?? ???????? ??????? ?????????.',
            '?? ???????? ??????? ??????????? ? ???????? ? ??????? ?????? ?? ?????? ????? ?????? ??? ????? ???. ???????????? ?????????? ???????? ???????????? ????? ?????????? ??????????.'
          )}
        </p>

        <GenderSelectionCards locale={locale} onSelectGender={onSelectGender} />
      </div>
    </div>
  );
}
















